holi
olap :D

no me dijiste ola denuevo :c

Paulap mirap:
IID (ya está) // Le hice unos cambios a la estructura, agregue hartas cositas y lo comprobe y funciona de pana
PID del sistema (falta)
Estado del proceso (RUNNING, STOPPED, ZOMBIE)
Tiempo de ejecución (uptime)
Ruta del binario ejecutado


y weno el gpt dice que falta todo esto aun del ps, te lo escribo aquipara que lo uses de guia:

NOTA – Estado actual del comando `ps`

El comando `ps` ya lista los procesos creados (se muestran los IID), pero está incompleto respecto a lo que exige la tarea. Falta implementar lo siguiente:

1. Mostrar información completa por proceso:

   * IID (ya está)
   * PID del sistema (falta)
   * Estado del proceso (RUNNING, STOPPED, ZOMBIE)
   * Tiempo de ejecución (uptime)
   * Ruta del binario ejecutado

2. Formato de salida:

   * Agregar encabezado tipo:
     IID PID STATE UPTIME BINARY
   * Mostrar cada proceso en una fila ordenada

3. Cálculo de uptime:

   * Usar `time(NULL)` menos el tiempo de inicio del proceso
   * Formatear a hh:mm:ss (ideal, pero opcional en primera versión)

4. Estados:

   * Asegurarse de mapear correctamente los estados internos a texto
   * Verificar que ZOMBIE se actualiza con `waitpid()`

5. Comunicación:

   * Actualmente el CLI solo muestra IID
   * Mejorar para que reciba y muestre toda la información
   * Alternativa rápida: imprimir directamente desde `toadd` (menos ideal pero funcional)

6. Validación:

   * Probar con múltiples procesos activos
   * Verificar comportamiento cuando un proceso termina (ZOMBIE)

Estado actual: funcional pero incompleto (aprox 60–70%).
Falta principalmente completar la información mostrada y mejorar el formato.

